[package]
name = "loop_ast_rs"
version = "0.1.0"
edition = "2021"

[dependencies]
syn = { version = "3", features = ["full", "visit"] }
proc-macro2 = { version = "1", features = ["span-locations"] }  # line/col outside macros
